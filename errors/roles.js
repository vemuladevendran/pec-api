"use strict";

const ADMIN = "admin";
const HOD = "hod";
const TEACHER = "teacher";
const STUDENT = "student";

const roles = [ADMIN, HOD, TEACHER, STUDENT];

module.exports = {
  ADMIN,
  HOD,
  TEACHER,
  STUDENT,
  roles,
};
